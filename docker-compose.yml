version: "3.9"

services:
  apimart-image-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: apimart-image-bridge
    restart: unless-stopped
    ports:
      - "43888:43888"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 43888
